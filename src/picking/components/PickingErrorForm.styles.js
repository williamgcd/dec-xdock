import styled from 'styled-components';

export const PickingErrorForm = styled.form`
   display: flex;
   flex-direction: column;
   margin: 1rem 0 0;

   ion-button {
      margin-top: 1rem;
   }

   ion-input {
      --padding-start: 0;
   }
`;
