import styled from 'styled-components';

export const Button = styled.button.attrs({
   className: 'Button',
})`
   --border-radius: 0;
   --box-shadow: none;

   font-weight: bold;
   height: 46px;
   margin: 0;
`;
